<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">
  <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="@dimen/picker_actionBarSize"
      android:background="@color/theme">
    <ImageView
        android:id="@+id/picker_back"
        android:layout_width="@dimen/picker_button_width"
        android:layout_height="@dimen/picker_button_height"
        android:scaleType="center"
        android:layout_centerVertical="true"
        android:src="@mipmap/arrow_left"/>
    <TextView
        android:id="@+id/picker_tv_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:textColor="@color/white"
        android:layout_toEndOf="@id/picker_back"
        android:layout_toRightOf="@id/picker_back"
        android:gravity="center_vertical"/>
  <Button
      android:id="@+id/btn_send"
      android:layout_width="@dimen/picker_button_width"
      android:layout_height="@dimen/picker_button_height"
      android:background="@drawable/btn_send_bg"
      android:enabled="false"
      android:layout_alignParentRight="true"
      android:layout_alignParentEnd="true"
      android:layout_marginRight="10dp"
      android:layout_marginEnd="10dp"
      android:layout_centerVertical="true"
      android:textColor="@color/gray_9"
      android:textAllCaps="false" />
  </RelativeLayout>
  <FrameLayout
      android:id="@+id/container"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      />

</LinearLayout>
